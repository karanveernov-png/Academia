# ──────────────────────────────────────────────────────────────────────
# Copy this file to `.streamlit/secrets.toml` (same folder) and put your
# real Groq API key below. `secrets.toml` is in .gitignore, so it will
# never be committed or pushed to GitHub — this is the "hidden" key.
#
# Get a free key at: https://console.groq.com/keys
#
# On Streamlit Community Cloud you don't need this file at all — just
# paste the same line into your app's "Settings → Secrets" box in the
# dashboard and it works the same way.
# ──────────────────────────────────────────────────────────────────────

GROQ_API_KEY = "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"
